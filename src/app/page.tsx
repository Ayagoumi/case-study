'use client';

import React from 'react';

import Navbar from '@/components/Navbar';

const Home: React.FC = () => {
  return (
    <main className="flex flex-1 flex-col">
      <Navbar />
    </main>
  );
};

export default Home;
